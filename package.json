{
  "name": "currentsensorwebapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1"
    // Add other dependencies here
  },
  "engines": {
    "node": ">=12.0.0"
    // Specify the Node.js version
  }
}
